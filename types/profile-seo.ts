/** Optional SEO overrides on CareerProfile — populated at publish or by AI later. */
export interface ProfileSeo {
  title?: string;
  description?: string;
  keywords?: string[];
  canonicalSlug?: string;
}
