/** Stage 7 — Professional Pitch. Architecture only; not implemented. */
export interface ProfessionalPitch {
  linkedInAbout: string;
  hhAbout: string;
  professionalBio: string;
  shortBio: string;
  speakerBio: string;
}
