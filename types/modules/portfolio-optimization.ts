/** Stage 5 — Portfolio Optimization. Architecture only; not implemented. */
export interface PortfolioOptimization {
  /** Section ids in priority order for the target vacancy. */
  sectionOrder: string[];
  emphasizedProjectIds: string[];
  emphasizedExperienceIndices: number[];
}
