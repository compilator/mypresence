# Copy to .env.local and fill in. Server-only; never commit real keys.

# OpenAI — used by the AI analysis Server Action (Milestone 3).
OPENAI_API_KEY=

# Public site URL for canonical links, OpenGraph, JSON-LD, and sitemap.
# NEXT_PUBLIC_SITE_URL=http://localhost:3000
# SITE_URL=http://localhost:3000

# Supabase — not used in the MVP; reserved for future auth/storage/db.
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
